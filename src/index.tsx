import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// A funcao render localiza o id root e insere um componente ou uma tag nela.
ReactDOM.render(
  <App />,
  document.getElementById('root')
);

